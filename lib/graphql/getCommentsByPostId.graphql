
query getCommentsByPostId($id : String!){
   getCommentsByPostId(postId: $id){
      content
       id
       isOffer
       postId
       userId
       userImageUrl
       username
       createdAt

   }
}