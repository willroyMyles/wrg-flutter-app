query getWatching($id: String!) {
  getWatching(id: $id) {
    ...p
  }
}

fragment p on Post {
  id
  category
  content
  createdAt
  make
  model
  title
  userId
  username
  userImageUrl
  views
  year
}
